# ---------------------------------------------------------------------------
# KYRO · configuración del frontend
#
# En desarrollo no hace falta nada: Vite reenvía /api, /uploads y /realtime al
# backend en localhost:4000.
# ---------------------------------------------------------------------------

# Origen del backend cuando NO se sirve desde el mismo dominio.
# Por ejemplo, con el frontend en Vercel y el backend en Railway/Render/Fly:
# VITE_API_URL=https://kyro-api.tu-dominio.com
